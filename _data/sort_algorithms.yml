# Sort benchmark metadata for _includes/sort-benchmark.md.
#
# When adding an algorithm:
# 1. Add a key here (id matches the `algorithm=` argument to sort-benchmark.md).
# 2. Set sort_fn to the Rust entry point included in benchmark_sort.
# 3. Set dependency flags only when the generated benchmark needs a shared helper
#    under _includes/sort-benchmark/helpers/, or when MAX_POWER must be capped
#    (`quadratic_average`; see _includes/sort-benchmark.md). Optional `max_power` caps
#    MAX_POWER below the quadratic default when an algorithm is slower than O(n²).
# 4. Add _includes/sort-benchmark/algorithms/<id>.rs with the algorithm implementation
#    (leave empty when sort_fn is only a shared helper, e.g. insertion/quick_lumoto/heap).

adaptive_shivers:
  sort_fn: adaptive_shivers_sort
  insertion_sort: true

american_flag:
  sort_fn: american_flag_sort
  insertion_sort: true

binary_insertion:
  sort_fn: binary_insertion_sort
  quadratic_average: true

binary_quick:
  sort_fn: binary_quick_sort
  insertion_sort: true

bingo:
  sort_fn: bingo_sort
  quadratic_average: true

binomial_heap:
  sort_fn: binomial_heap_sort

bitonic:
  sort_fn: bitonic_sort

brick:
  sort_fn: brick_sort
  quadratic_average: true

bubble:
  sort_fn: bubble_sort
  quadratic_average: true

bucket:
  sort_fn: bucket_sort
  insertion_sort: true

burst:
  sort_fn: burst_sort
  insertion_sort: true

cartesian_tree:
  sort_fn: cartesian_tree_sort
  merge_values: true

cascade_merge:
  sort_fn: cascade_merge_sort

circle:
  sort_fn: circle_sort

comb:
  sort_fn: comb_sort

counting:
  sort_fn: counting_sort

cradix:
  sort_fn: cradix_sort

crum:
  sort_fn: crum_sort
  insertion_sort: true

cycle:
  sort_fn: cycle_sort
  quadratic_average: true

dual_pivot_quick:
  sort_fn: dual_pivot_quick_sort
  insertion_sort: true

exchange:
  sort_fn: exchange_sort
  quadratic_average: true

fibonacci_heap:
  sort_fn: fibonacci_heap_sort

flash:
  sort_fn: flash_sort
  insertion_sort: true

flux:
  sort_fn: flux_sort
  insertion_sort: true

ford_johnson:
  sort_fn: ford_johnson
  quadratic_average: true

franceschini:
  sort_fn: franceschini_sort

funnel:
  sort_fn: funnel_sort
  insertion_sort: true

gnome:
  sort_fn: gnome_sort
  quadratic_average: true

grail:
  sort_fn: grail_sort

gravity:
  sort_fn: gravity_sort
  quadratic_average: true

heap:
  sort_fn: heap_sort
  heap_sort: true

insertion:
  sort_fn: insertion_sort
  insertion_sort: true
  quadratic_average: true

intro:
  sort_fn: intro_sort
  insertion_sort: true
  partition_at: true
  partition: true
  heap_sort: true

kota:
  sort_fn: kota_sort

leftist_heap:
  sort_fn: leftist_heap_sort

library:
  sort_fn: library_sort

loser_tree:
  sort_fn: loser_tree_sort

merge:
  sort_fn: merge_sort

natural_merge:
  sort_fn: natural_merge_sort

oddeven_merge:
  sort_fn: odd_even_merge_sort

pancake:
  sort_fn: pancake_sort
  quadratic_average: true

pairing_heap:
  sort_fn: pairing_heap_sort

patience:
  sort_fn: patience_sort

pattern_defeating_quick:
  sort_fn: pattern_defeating_quick_sort
  insertion_sort: true
  heap_sort: true

pigeonhole:
  sort_fn: pigeonhole_sort

polyphase_merge:
  sort_fn: polyphase_merge_sort

postman:
  sort_fn: postman_sort
  insertion_sort: true

power:
  sort_fn: power_sort
  insertion_sort: true

proportion_extend:
  sort_fn: proportion_extend_sort
  insertion_sort: true
  partition_at: true

proxmap:
  sort_fn: proxmap_sort

quad:
  sort_fn: quad_sort

quick_hoare:
  sort_fn: quick_hoare_sort
  insertion_sort: true

quick_lumoto:
  sort_fn: quick_sort
  insertion_sort: true
  partition_at: true
  partition: true
  quick_sort: true

quick_median_of_three:
  sort_fn: quick_median_of_three_sort
  insertion_sort: true
  partition_at: true

quick_random_pivot:
  sort_fn: quick_random_pivot_sort
  insertion_sort: true
  partition_at: true

radix:
  sort_fn: radix_sort

sample:
  sort_fn: sample_sort
  insertion_sort: true
  partition_at: true
  partition: true
  quick_sort: true

selection:
  sort_fn: selection_sort
  quadratic_average: true

self_indexed:
  sort_fn: self_indexed_sort

shaker:
  sort_fn: shaker_sort
  quadratic_average: true

shear:
  sort_fn: shear_sort
  insertion_sort: true

shell:
  sort_fn: shell_sort

shivers:
  sort_fn: shivers_sort
  insertion_sort: true

shuttle:
  sort_fn: shuttle_sort
  insertion_sort: true
  quadratic_average: true

sleep:
  sort_fn: sleep_sort
  quadratic_average: true
  max_power: 8

slow:
  sort_fn: slow_sort
  quadratic_average: true
  max_power: 8

smooth:
  sort_fn: smooth_sort

splay:
  sort_fn: splay_sort

spread:
  sort_fn: spread_sort
  insertion_sort: true

stooge:
  sort_fn: stooge_sort
  quadratic_average: true
  max_power: 11

strand:
  sort_fn: strand_sort
  merge_values: true

symmetry_partition:
  sort_fn: symmetry_partition_sort
  insertion_sort: true
  partition_at: true

three_way_quick:
  sort_fn: three_way_quick_sort
  insertion_sort: true

tim:
  sort_fn: tim_sort
  insertion_sort: true

tournament:
  sort_fn: tournament_sort

tree:
  sort_fn: tree_sort

trie:
  sort_fn: trie_sort

two_way_insertion:
  sort_fn: two_way_insertion_sort
  quadratic_average: true

unshuffle:
  sort_fn: unshuffle_sort

van_emde_boas:
  sort_fn: van_emde_boas_sort

weak_heap:
  sort_fn: weak_heap_sort

wiki:
  sort_fn: wiki_sort
