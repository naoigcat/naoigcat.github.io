---
layout: post
title:  二分木ソートで配列を並び替える
date:   2026-05-12 04:47:29 +0900
tags:   sort
---

## 二分木ソートを使用する

二分木ソート (`tree sort`) は、要素を順に **二分探索木 (`binary search tree`)** へ挿入し、その後**中順走査 (`in-order traversal`; 左部分木 → 根 → 右部分木)** ですべてのキーを読み出して昇順を得る比較ソートである。

通常の二分探索木は挿入順によって木の高さが偏りやすく、特定の入力では高さが O(n) になり、結果として時間計算量が **O(n²)** に悪化する。

そこで **平衡二分探索木 (`self-balancing binary search tree`)**を使うと、木の高さを O(log n) に抑えられる。

平衡木への各挿入は高さ O(log n) の探索と再平衡化を伴うため、挿入全体で O(n log n) となる。一方、中順走査は左部分木、根、右部分木の順に各ノードを一度ずつ処理するだけなので全体でO(n) である。

したがって、合計は O(n log n) + O(n) = O(n log n) となる。別途 **木全体のために O(n) のノード記憶域**が必要になり、入力配列とは別構造となる点はインプレースではない。

また、**キー（ここでは棒の高さ＝値）だけ**で比較する素のツリーソートでは、等しいキー同士の相対順序は木の実装や等値を左子・右子のどちらへ入れるかなどの規約に依存し、入力順は保証されない。したがって一般には **安定ソートではない**。安定性まで要るなら、比較へ到着順などの副キーを含める必要がある（これはアルゴリズムの拡張であり、「通常の」キー比較だけの性質とは別物である）。

次に掲げるデモでは、同じ値の棒が画面上で入れ替わらないよう、二分木挿入の比較を **値が異なれば値、等しければ元の位置 `id`** の辞書式順にしている。これは **本ページの可視化のための工夫** であり、直前の段落で述べた「素のツリーソートは一般に不安定」という話を打ち消すものではない。

1.  **挿入**: 入力値を順に平衡二分探索木へ挿入する（規則に従って回転や再着色などでバランスを復元する）。
2.  **取出し**: 中順走査でキーを昇順に列挙し、ひとつの配列へ書き込むか、そのまま消費する。

```pseudocode
procedure balanced_tree_sort(elements)
  T = empty balanced binary search tree
  for x in elements
    insert_balanced(T, x)
  return inorder_traversal(T)
```

単純な二分木ソートだけでコードは短くても入力に弱いので、実務や学習用の標準とはいえにくく、しかし二分木を使って整列させるという考え方自体はアルゴリズムの説明および他の資料構造（優先度付きキューのヒープ整列との対比など）において有用である。

<!-- markdownlint-disable MD046 -->
<div id="tree-sort-demo" class="tree-sort-demo">
<style>
.tree-sort-demo {
  margin: 1.25rem 0;
  padding: 1rem;
  border: 1px solid rgba(128,128,128,.35);
  border-radius: 8px;
  background: var(--minima-brand-color-lightest, #f9f9f9);
}
.tree-sort-demo__toolbar {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem 1rem;
  align-items: center;
  margin-bottom: 0.75rem;
  font-size: 0.9rem;
}
.tree-sort-demo__toolbar button {
  padding: 0.35rem 0.65rem;
  border-radius: 6px;
  border: 1px solid rgba(0,0,0,.2);
  background: #fff;
  cursor: pointer;
  font: inherit;
}
.tree-sort-demo__toolbar button:hover {
  border-color: rgba(0,0,0,.45);
}
.tree-sort-demo__toolbar button:disabled {
  opacity: 0.45;
  cursor: not-allowed;
}
.tree-sort-demo__bars {
  display: flex;
  align-items: flex-end;
  justify-content: center;
  gap: 6px;
  min-height: 140px;
  padding: 0.5rem;
}
.tree-sort-demo__bar {
  flex: 1 1 0;
  max-width: 48px;
  min-width: 28px;
  border-radius: 4px 4px 2px 2px;
  background: linear-gradient(180deg, #5b9bd5 0%, #2e75b6 100%);
  box-shadow: 0 2px 4px rgba(0,0,0,.12);
  transition: box-shadow 0.15s ease, outline-color 0.15s ease;
  transform: translateX(0);
}
.tree-sort-demo__bar[data-role="insert"] {
  outline: 3px solid #8e44ad;
  outline-offset: 2px;
  box-shadow: 0 0 0 2px rgba(142,68,173,.35), 0 2px 6px rgba(0,0,0,.18);
}
.tree-sort-demo__bar[data-role="swap"] {
  outline: 3px solid #27ae60;
  outline-offset: 2px;
}
.tree-sort-demo__caption { margin-top: 0.5rem; font-size: 0.85rem; color: #555; text-align: center; min-height: 1.25em; }
@media (prefers-color-scheme: dark) {
  .tree-sort-demo { background: rgba(255,255,255,.06); border-color: rgba(255,255,255,.18); }
  .tree-sort-demo__toolbar button { background: rgba(255,255,255,.08); border-color: rgba(255,255,255,.25); color: inherit; }
  .tree-sort-demo__caption { color: #bbb; }
}
</style>
<div class="tree-sort-demo__toolbar">
  <button type="button" data-ts="shuffle">シャッフル</button>
  <button type="button" data-ts="play">自動再生</button>
  <button type="button" data-ts="pause" disabled>一時停止</button>
  <button type="button" data-ts="step">1ステップ</button>
</div>
<div class="tree-sort-demo__bars" data-ts="bars" aria-live="polite"></div>
<p class="tree-sort-demo__caption" data-ts="caption"></p>
<script src="{{ '/assets/js/demo-sort.js' | relative_url }}"></script>
<script>
(function () {
  var root = document.getElementById('tree-sort-demo');
  if (!root) return;
  var C = window.DemoSort;
  if (!C) return;

  function cmp(a, b) {
    if (a.value !== b.value) return a.value < b.value ? -1 : 1;
    if (a.id !== b.id) return a.id < b.id ? -1 : 1;
    return 0;
  }

  function height(n) {
    return n ? n.h : 0;
  }

  function rotRight(y) {
    var x = y.left;
    var t2 = x.right;
    x.right = y;
    y.left = t2;
    y.h = 1 + Math.max(height(y.left), height(y.right));
    x.h = 1 + Math.max(height(x.left), height(x.right));
    return x;
  }

  function rotLeft(x) {
    var y = x.right;
    var t2 = y.left;
    y.left = x;
    x.right = t2;
    x.h = 1 + Math.max(height(x.left), height(x.right));
    y.h = 1 + Math.max(height(y.left), height(y.right));
    return y;
  }

  function balanceBF(n) {
    return height(n.left) - height(n.right);
  }

  function avlInsert(n, item) {
    if (!n) {
      return { k: item, left: null, right: null, h: 1 };
    }
    var c = cmp(item, n.k);
    if (c < 0) n.left = avlInsert(n.left, item);
    else if (c > 0) n.right = avlInsert(n.right, item);
    else return n;

    n.h = 1 + Math.max(height(n.left), height(n.right));
    var bal = balanceBF(n);

    if (bal > 1 && cmp(item, n.left.k) < 0) {
      return rotRight(n);
    }
    if (bal < -1 && cmp(item, n.right.k) > 0) {
      return rotLeft(n);
    }
    if (bal > 1 && cmp(item, n.left.k) > 0) {
      n.left = rotLeft(n.left);
      return rotRight(n);
    }
    if (bal < -1 && cmp(item, n.right.k) < 0) {
      n.right = rotRight(n.right);
      return rotLeft(n);
    }
    return n;
  }

  function inorderCollect(n, out) {
    if (!n) return;
    inorderCollect(n.left, out);
    out.push(n.k);
    inorderCollect(n.right, out);
  }

  function generateSteps(vals) {
    var steps = [];
    var ids = vals.map(function (v, i) {
      return { value: v, id: i };
    });

    steps.push({
      kind: 'phase_insert',
      arr: vals.slice()
    });

    var avlRoot = null;
    for (var i = 0; i < ids.length; i++) {
      steps.push({
        kind: 'insert',
        idx: i,
        value: vals[i],
        arr: vals.slice()
      });
      avlRoot = avlInsert(avlRoot, ids[i]);
    }

    var sortedKeys = [];
    inorderCollect(avlRoot, sortedKeys);
    var sortedIds = sortedKeys.map(function (x) {
      return x.id;
    });

    steps.push({
      kind: 'phase_reorder',
      caption: '中順走査の順（昇順）に並べ替えます（スワップで視覚化）'
    });

    var n = sortedIds.length;
    var perm = [];
    for (var pj = 0; pj < n; pj++) {
      perm[pj] = pj;
    }

    function posOf(origId, p) {
      for (var q = 0; q < p.length; q++) {
        if (p[q] === origId) {
          return q;
        }
      }
      return -1;
    }

    for (var tgt = 0; tgt < n - 1; tgt++) {
      var wantOrig = sortedIds[tgt];
      if (perm[tgt] === wantOrig) continue;
      var src = posOf(wantOrig, perm);
      steps.push({
        kind: 'swap',
        lo: tgt,
        hi: src,
        caption:
          '位置 ' + tgt + ' と ' + src + ' を入れ替え（整列済み順へ反映）'
      });
      var a = perm[tgt];
      var b = perm[src];
      perm[tgt] = b;
      perm[src] = a;
    }

    steps.push({ kind: 'done' });

    return steps;
  }

  function mountBars(container, values, orderByOriginalIndex) {
    container.innerHTML = '';
    if (!values.length) return;
    var max = Math.max.apply(null, values);
    var min = Math.min.apply(null, values);
    var span = Math.max(max - min, 1);
    var order = [];
    if (orderByOriginalIndex && orderByOriginalIndex.length === values.length) {
      order = orderByOriginalIndex.slice();
    } else {
      for (var ii = 0; ii < values.length; ii++) order.push(ii);
    }
    order.forEach(function (origIdx) {
      var v = values[origIdx];
      var bar = document.createElement('div');
      bar.className = 'tree-sort-demo__bar';
      bar.dataset.origIndex = String(origIdx);
      var h = 28 + ((v - min) / span) * 92;
      bar.style.height = h + 'px';
      bar.setAttribute('title', String(v));
      container.appendChild(bar);
    });
  }

  function clearRoles(container) {
    var nodes = container.children;
    for (var i = 0; i < nodes.length; i++) {
      nodes[i].removeAttribute('data-role');
    }
  }

  function setInsertRole(container, origIndex) {
    clearRoles(container);
    var nodes = container.children;
    for (var ni = 0; ni < nodes.length; ni++) {
      if (nodes[ni].dataset.origIndex === String(origIndex)) {
        nodes[ni].setAttribute('data-role', 'insert');
        break;
      }
    }
  }

  function swapRole(container, lo, hi) {
    clearRoles(container);
    var nodes = container.children;
    if (nodes[lo]) nodes[lo].setAttribute('data-role', 'swap');
    if (nodes[hi]) nodes[hi].setAttribute('data-role', 'swap');
  }

  var barsEl = root.querySelector('[data-ts="bars"]');
  var capEl = root.querySelector('[data-ts="caption"]');
  var btnShuffle = root.querySelector('[data-ts="shuffle"]');
  var btnPlay = root.querySelector('[data-ts="play"]');
  var btnPause = root.querySelector('[data-ts="pause"]');
  var btnStep = root.querySelector('[data-ts="step"]');

  var values = [5, 2, 8, 1, 9, 3, 6, 14, 4, 11, 7, 13, 10, 12, 15];
  var steps = [];
  var idx = 0;
  var playing = false;
  var cancelled = false;
  var busy = false;
  var currentOrderPerm = [];

  function rebuildOrderFromPerm(masterLen, perm) {
    currentOrderPerm = perm.slice();
  }

  function syncButtons() {
    var atEnd = idx >= steps.length;
    btnPlay.disabled = playing || atEnd || busy;
    btnPause.disabled = !playing;
    btnStep.disabled = playing || atEnd || busy;
    btnShuffle.disabled = playing;
  }

  function rebuild(v) {
    values = v.slice();
    steps = generateSteps(values);
    idx = 0;
    cancelled = true;
    playing = false;
    busy = false;
    var order = [];
    for (var oi = 0; oi < values.length; oi++) order.push(oi);
    rebuildOrderFromPerm(values.length, order);
    mountBars(barsEl, values, currentOrderPerm);
    capEl.textContent =
      'ツリーソート（デモは AVL 挿入＋並び順の復元）。挿入は紫、スワップは緑です。';
    syncButtons();
  }

  async function applyStepForward() {
    if (busy || idx >= steps.length) return;
    busy = true;
    syncButtons();
    try {
      var s = steps[idx];
      idx++;

      if (s.kind === 'phase_insert') {
        mountBars(barsEl, values, currentOrderPerm);
        clearRoles(barsEl);
        capEl.textContent = '1. 入力順に値を AVL 木へ挿入していきます（木は内部のみ）';
        return;
      }

      if (s.kind === 'insert') {
        mountBars(barsEl, values, currentOrderPerm);
        setInsertRole(barsEl, s.idx);
        capEl.textContent = 'AVL に挿入: 入力位置 ' + s.idx + ' の値（' + s.value + '）';
        return;
      }

      if (s.kind === 'phase_reorder') {
        mountBars(barsEl, values, currentOrderPerm);
        clearRoles(barsEl);
        capEl.textContent = s.caption;
        return;
      }

      if (s.kind === 'swap') {
        swapRole(barsEl, s.lo, s.hi);
        capEl.textContent = '交換中… (' + s.caption + ')';
        await C.flipSwap(barsEl, s.lo, s.hi);

        var newPerm = [];
        for (var cj = 0; cj < barsEl.children.length; cj++) {
          newPerm.push(parseInt(barsEl.children[cj].dataset.origIndex, 10));
        }
        rebuildOrderFromPerm(values.length, newPerm);

        clearRoles(barsEl);
        capEl.textContent = '交換完了';
        return;
      }

      if (s.kind === 'done') {
        mountBars(barsEl, values, currentOrderPerm);
        clearRoles(barsEl);
        capEl.textContent = 'ソート完了（左から昇順）';
      }
    } finally {
      busy = false;
      syncButtons();
    }
  }

  btnShuffle.addEventListener('click', function () {
    rebuild(C.shuffleCopy(values));
  });

  btnStep.addEventListener('click', function () {
    applyStepForward();
  });

  btnPlay.addEventListener('click', async function () {
    playing = true;
    cancelled = false;
    syncButtons();
    while (!cancelled && idx < steps.length) {
      await applyStepForward();
      await C.wait(idx < steps.length && steps[idx].kind === 'swap' ? 340 : 220);
    }
    playing = false;
    syncButtons();
  });

  btnPause.addEventListener('click', function () {
    cancelled = true;
    playing = false;
    syncButtons();
  });

  rebuild(values);
})();
</script>
</div>
<!-- markdownlint-enable MD046 -->

クイックソートのようなインプレース志向の手法と異なり、ここでのデモでも **木の論理とは別に配列側の並びは最後まで入れ替えない** と説明できないので、視覚化のために中順順序へ **スワップで寄せている**。実装では通常、走査結果を別バッファに書き込むだけで済ませればよい。バブルソートより改善されやすい計算量一方で、キャッシュ効率や定数係数、アロケータへの負荷などの観点では配列だけを直接いじる整列アルゴリズムに軍配が挙がることもある。
